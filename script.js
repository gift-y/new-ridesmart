document.addEventListener('DOMContentLoaded', function() { // Review Carousel Functionality const reviewsContainer = document.getElementById('reviewsContainer'); const prevBtn = document.getElementById('prevBtn'); const nextBtn = document.getElementById('nextBtn'); // Sample review data const reviews = [ { stars: 3, title: "Driver was very helpful and...", text: "Driver was very helpful and polite. Safe drive all the way in tricky...", author: "Jael", time: "16 hours ago" }, { stars: 3, title: "Excellent service!", text: "The driver was punctual and professional. Would recommend...", author: "Sarah", time: "2 days ago" }, { stars: 3, title: "Great experience", text: "Very comfortable ride and friendly driver. Will use again...", author: "Mike", time: "1 day ago" }, { stars: 3, title: "Reliable transportation", text: "Always on time and very professional service...", author: "Emma", time: "3 days ago" } ]; // Populate reviews function createReviewCard(review) { return `
    ${Array(review.stars).fill('⭐').join('')}
    ${review.title}
    ${review.text}
    
    ${review.author}, ${review.time}
    `; } reviewsContainer.innerHTML = reviews.map(createReviewCard).join(''); // Carousel Navigation let scrollPosition = 0; const cardWidth = reviewsContainer.querySelector('.review-card').offsetWidth + 16; // 16px for gap nextBtn.addEventListener('click', () => { scrollPosition += cardWidth; if (scrollPosition > cardWidth * (reviews.length - 3)) { scrollPosition = 0; } reviewsContainer.scrollTo({ left: scrollPosition, behavior: 'smooth' }); }); prevBtn.addEventListener('click', () => { scrollPosition -= cardWidth; if (scrollPosition < 0) { scrollPosition = cardWidth * (reviews.length - 3); } reviewsContainer.scrollTo({ left: scrollPosition, behavior: 'smooth' }); }); // Search Form Functionality const searchForm = document.getElementById('searchForm'); const datePicker = document.getElementById('datePicker'); // Simple date picker initialization datePicker.addEventListener('click', () => { datePicker.type = 'date'; }); datePicker.addEventListener('blur', () => { if (!datePicker.value) { datePicker.type = 'text'; } }); // Form submission searchForm.addEventListener('submit', (e) => { e.preventDefault(); const formData = new FormData(searchForm); const searchData = Object.fromEntries(formData); // Here you would typically send this data to your backend console.log('Search Data:', searchData); alert('Search submitted! Check console for details.'); }); // Responsive Navigation window.addEventListener('resize', () => { scrollPosition = 0; reviewsContainer.scrollTo({ left: 0, behavior: 'smooth' }); }); });